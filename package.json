{
  "name": "boids",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Boids - an installation. Curation pipeline (beloved->reviled axis) + rough live flocking preview.",
  "scripts": {
    "curate": "tsx src/curation/curate.ts",
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "tsx --test src/**/*.test.ts"
  },
  "devDependencies": {
    "@types/node": "^26.1.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vite": "^6.0.5"
  }
}
